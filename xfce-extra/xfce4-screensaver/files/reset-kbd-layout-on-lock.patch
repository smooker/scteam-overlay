diff --git a/src/gs-lock-plug.c b/src/gs-lock-plug.c
index 15f59ae..bc8b65f 100644
--- a/src/gs-lock-plug.c
+++ b/src/gs-lock-plug.c
@@ -1659,6 +1659,10 @@ gs_lock_plug_init (GSLockPlug *plug) {
             gtk_widget_hide (plug->priv->auth_prompt_kbd_layout_indicator);
         }
 
+        /* Reset to first keyboard layout group (e.g. US) so the password
+         * prompt always starts with the default layout */
+        xkl_engine_lock_group (engine, 0);
+
         g_object_unref (engine);
     }
 #endif
